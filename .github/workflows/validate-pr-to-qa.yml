name: Deployment to QA
on:
  pull_request:
    types:
      - opened
      - synchronize
    paths:
      - force-app/**
    branches:
      - Integration

permissions:
  actions: read
  contents: read
  security-events: write

jobs:
  validate-pr-to-qa:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout source code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Install NPM
        run: |
          npm install --legacy-peer-deps
      - name: Install Salesforce CLI
        run: |
          wget https://developer.salesforce.com/media/salesforce-cli/sfdx/channels/stable/sfdx-linux-x64.tar.xz
          mkdir ~/sfdx
          tar xJf sfdx-linux-x64.tar.xz -C ~/sfdx --strip-components 1
          echo "$HOME/sfdx/bin" >> $GITHUB_PATH
          ~/sfdx/bin/sfdx version
      - name: Populate auth file with SFDX_URL secret of target org
        run: |
          echo ${{ secrets.QA_SFDX_URL}} > ./QA_SFDX_URL.txt
      - name: Authenticate to target Org
        run: sf org login sfdx-url -f ./QA_SFDX_URL.txt --alias targetOrg --set-default
      - name: Installing sfdx git delta
        run: |
          echo y | sf plugins install sfdx-git-delta
          sf plugins
      - name: Create delta packages for new, modified or deleted metadata
        run: |
          mkdir changed-sources
          sf sgd source delta -f "HEAD^" -t "HEAD" -o "changed-sources/" -d -s "force-app/"
      - name: Installing java
        run: sudo apt-get update && sudo apt-get install default-jdk
      - name: Installing SFDX scanner
        run: sf plugins install @salesforce/sfdx-scanner@v3.26.0
      - name: Scan code
        run: sf scanner run --format sarif --target 'changed-sources/**/*.cls' --category 'Best Practices,Security' --outfile 'changed-sources/apexScanResults.sarif'
      - name: List files in changed-sources directory
        run: ls -al changed-sources/
      - name: Upload SARIF file
        uses: github/codeql-action/upload-sarif@v2
        with:
          sarif_file: changed-sources/apexScanResults.sarif
      - name: Deployment - run no tests
        run: >
          sf project deploy start --source-dir "changed-sources/force-app" --test-level NoTestRun --dry-run --json
      - name: 'validate destructive changes (if any)'
        run: sf project deploy start --metadata-dir "changed-sources/destructiveChanges" --test-level NoTestRun --dry-run --ignore-errors
